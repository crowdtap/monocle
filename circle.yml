general:
  branches:
    ignore:
      - production
machine:
  timezone:
    America/New_York
dependencies:
  pre:
    - wget http://www.kokkonen.net/tjko/src/jpegoptim-1.3.0.tar.gz
    - zcat jpegoptim-1.3.0.tar.gz | tar xf -
    - cd jpegoptim-1.3.0 && ./configure && make
deployment:
  production:
    branch: master
    commands:
      - curl -H 'x-api-key:20dca2067c57c415fd797184715b1b' -XPOST http://deployer.crowdtap.com/deploys/$CIRCLE_PROJECT_REPONAME/production/$CIRCLE_SHA1
