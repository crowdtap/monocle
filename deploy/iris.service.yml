---
apiVersion: v1
kind: Service
metadata:
  name: iris
  labels:
    app: iris
spec:
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: iris
